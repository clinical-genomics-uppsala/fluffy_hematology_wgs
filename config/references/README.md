Reference bundles for this pipeline should live in this directory.

The current flat reference files are kept during the migration, but the shared layout expects site-specific reference YAMLs to be grouped here.
